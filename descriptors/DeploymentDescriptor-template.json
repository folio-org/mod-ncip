{
  "srvcId": "${artifactId}-${version}",
  "nodeId": "localhost",
  "descriptor": {
    "exec": "java -Dport=%p -Dhttp.port=%p -Dlog_level=debug -jar ../${artifactId}/target/${artifactId}-fat.jar"
  }
}