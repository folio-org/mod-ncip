{
  "srvcId": "${artifactId}-${version}",
  "nodeId": "localhost",
  "descriptor": {
    "exec": "java -jar -jar  -Dport=%p -Dhttp.port=%p -Dlog_level=debug ../${artifactId}/target/${artifactId}-fat.jar"
  }
}
